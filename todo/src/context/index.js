//by using useTodo all elements incide of TodoContext can be accessed so that we donot need to 
//keep exporting all elements from TodoContext

export { TodoContext, Todoprovider, useTodo } from "./Context";
